{
  "name": "@ebowwa/codegen-kit",
  "version": "1.5.0",
  "description": "Shared generate/validate/check scaffolding for codegen — extracted from SecondSee's node-codegen. Domain-agnostic: any repo declares its generators + validators on top and gets write/--check/drift/umbrella machinery.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./schema": {
      "import": "./dist/schema/index.js",
      "types": "./dist/schema/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit -p tsconfig.test.json",
    "prepublishOnly": "bun run build",
    "demo:shapes": "bun run examples/shapes-demo.ts",
    "demo:codegen": "bun run examples/codegen-demo.ts",
    "demo:probe": "bun run examples/custom-probe-demo.ts",
    "demo:systems": "bun run examples/systems-demo.ts",
    "demo": "bun run examples/shapes-demo.ts && bun run examples/codegen-demo.ts && bun run examples/custom-probe-demo.ts && bun run examples/systems-demo.ts"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ebowwa/codegen-kit.git"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/bun": "^1.1.0"
  },
  "license": "MIT"
}
